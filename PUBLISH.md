# Publish Configuration for vendure-mcp-graphql

<!-- Auto-generated by publish-manager skill on 2026-07-13. Edit if the workflow changes. -->

## Package Manager

- **Type**: npm
- **Packages**: 1 — `vendure-mcp-graphql`
- **Public packages**: `vendure-mcp-graphql` (published to npmjs.com)

## Scripts

- **Pre-publish**: Update version in `package.json`, update `CHANGELOG.md`, run `npx tsc` to build `dist/`
- **Publish**: `npm publish`

## Workflow

1. Update version in `package.json`
2. Update `CHANGELOG.md` with new version entry
3. Run `npx tsc` to build
4. Commit and push to `publish-<version>` branch
5. `npm publish`

## Notes

- The `dist/` directory is in `.gitignore` but is included in the npm package via `"files": ["dist"]` in `package.json`.
- The package uses ESM (`"type": "module"`).
- No pre-publish or post-publish scripts exist.
